---
import type { ImageMetadata } from 'astro';
import { Image } from 'astro:assets';

interface Props {
  content: {
    eyebrow: string;
    titleLines: readonly string[];
    leadLines: readonly string[];
    images: readonly {
      src: ImageMetadata;
      alt: string;
    }[];
  };
}

const { content } = Astro.props;
---

<section class="culture section" id="culture">
  <div class="culture__heading container">
    <div class="section-heading js-reveal">
      <p class="eyebrow">{content.eyebrow}</p>
      <h2 class="section-title">
        {
          content.titleLines.map((line) => (
            <span class="title-line">{line}</span>
          ))
        }
      </h2>
      <p class="section-lead">
        {
          content.leadLines.map((line) => (
            <span class="culture__lead-line">{line}</span>
          ))
        }
      </p>
    </div>
  </div>

  <div class="culture__gallery" data-gallery>
    {
      content.images.map((image, index) => (
        <figure class:list={['culture__item', `culture__item--${index + 1}`, 'js-gallery-item']}>
          <Image
            class="culture__image"
            src={image.src}
            alt={image.alt}
            loading="lazy"
            quality={84}
            widths={[420, 640, 960]}
            sizes="(max-width: 767px) 72vw, 34vw"
          />
          <span class="culture__index" aria-hidden="true">
            {String(index + 1).padStart(2, '0')}
          </span>
        </figure>
      ))
    }
  </div>
</section>

<style lang="scss">
  .culture {
    padding-bottom: calc(var(--section-space) + 2rem);
    background:
      radial-gradient(circle at 12% 10%, rgba(142, 216, 230, 0.22), transparent 28%),
      linear-gradient(180deg, var(--color-ivory) 0%, #ffffff 100%);

    &__lead-line {
      display: block;
    }

    &__gallery {
      display: grid;
      grid-template-columns: repeat(12, minmax(0, 1fr));
      grid-auto-rows: 3.15vw;
      gap: 1rem;
      width: min(calc(100% - 2rem), 1440px);
      margin-inline: auto;
    }

    &__item {
      position: relative;
      overflow: hidden;
      margin: 0;
      background: var(--color-cream);

      &::after {
        position: absolute;
        inset: 0;
        border: 1px solid rgba(32, 50, 56, 0.1);
        box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.28);
        content: "";
        pointer-events: none;
      }

      &--1 {
        grid-area: 1 / 1 / span 7 / span 4;
      }

      &--2 {
        grid-area: 2 / 5 / span 6 / span 5;
      }

      &--3 {
        grid-area: 1 / 10 / span 8 / span 3;
      }

      &--4 {
        grid-area: 8 / 1 / span 6 / span 5;
      }

      &--5 {
        grid-area: 8 / 6 / span 8 / span 3;
      }

      &--6 {
        grid-area: 9 / 9 / span 6 / span 4;
      }

      &--7 {
        grid-area: 14 / 1 / span 5 / span 4;
      }

      &--8 {
        grid-area: 16 / 5 / span 5 / span 5;
      }

      &--9 {
        grid-area: 16 / 10 / span 5 / span 3;
      }
    }

    &__image {
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center 40%;
      transition:
        transform 0.8s cubic-bezier(0.22, 1, 0.36, 1),
        filter 0.5s ease;
    }

    &__item:hover &__image {
      filter: saturate(1.15);
      transform: scale(1.02);
    }

    &__index {
      position: absolute;
      z-index: 1;
      right: 0.8rem;
      bottom: 0.55rem;
      display: grid;
      width: 2rem;
      height: 2rem;
      place-items: center;
      border-radius: 50%;
      color: var(--color-ink);
      background: var(--color-gold);
      font-family: var(--font-display);
      font-size: 0.55rem;
      font-weight: 900;
    }
  }

  @media (max-width: 767px) {
    .culture {
      :global(.title-line) {
        white-space: normal;
      }

      &__gallery {
        display: flex;
        width: 100%;
        gap: 0.8rem;
        overflow-x: auto;
        padding: 0 1rem 1.5rem;
        scroll-snap-type: x mandatory;
        scrollbar-width: none;
      }

      &__gallery::-webkit-scrollbar {
        display: none;
      }

      &__item {
        width: min(68vw, 18.5rem);
        flex: 0 0 auto;
        aspect-ratio: 3 / 4;
        scroll-snap-align: center;
      }
    }
  }
</style>
